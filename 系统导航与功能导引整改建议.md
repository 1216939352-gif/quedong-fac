# 系统导航与功能导引整改建议

> 目标：让用户（医生/管理员）一眼看懂系统内置功能、降低培训成本。
> 分析对象：`app.js` 中 `NAV`（左侧系统栏）、`index.html` 路由、`#/plan` 与 `#/sarcopenia` 两条方案主线的关系。

---

## 一、现状左侧系统栏（NAV）结构

当前分 7 段、约 22 项：

| 分组 | 功能项 |
|---|---|
| 临床业务流程 | 工作台首页 / 患者首诊登记 / 综合评估 / 生活方式问卷评估 |
| 肌力专项测评 | 等速肌力评估 / 等张肌力评估 |
| 综合方案 | 智能方案生成（`#/plan`） |
| 老年人体重与肌少症管理 | 肌少症专项台账 / 肌少症标准化评估 / 肌少症数据看板 |
| 报告管理中心 | 报告管理中心 / 大数据展示 |
| 系统设置 | 鹊动设备档案 / 运动方案库管理中心(admin) / 系统管理后台(admin) |
| 资讯与消息推送 | 资讯管理(admin) / 系统消息管理(admin) / 医生分组与接收人(admin) / 资讯中心(doctor) / 系统消息中心(doctor) |

---

## 二、模块关联梳理（肌少症 ↔ 主系统）

### 2.1 共享基础设施（同一套底座）
- 登录/权限（admin / doctor）、患者主档、IndexedDB 数据层。
- 综合评估（握力/体成分/SPPB/跌倒等）——**肌少症评估复用其指标**。
- 肌力测评：等速、等张（OCR 已修复，1RM 可正确抽取）——**两模块都依赖其产出**。
- 设备档案 `CONST.DEVICES`（9 台）、方案库 `StrengthLib`(32) + `SarcExerciseLib`(36)。
- 报告中心、后台、资讯/消息。

### 2.2 两条方案主线的关系（核心混淆点）
- **`#/plan` 智能方案生成**：面向「体重管理」通用人群，由主线 `plan.js` + `PlanEngine`（严谨版）生成，含鹊动设备处方 + 居家徒手六模式。
- **`#/sarcopenia` 肌少症模块**：面向「老年肌少症」专病，独立台账/评估/报告/随访，其方案由 `SarcEngine` + `SarcExerciseLib` 生成。
- **数据流向**：`#/assessment` / `#/isokinetic` / `#/isotonic` 的评估结果 → 同时喂给 `#/plan`（体重管理方案）与 `#/sarcopenia`（肌少症方案）。
- **问题**：用户在左侧同时看到「综合方案 / 智能方案生成」与「老年人体重与肌少症管理 / 肌少症专项台账」两个都能出方案的地方，不清楚**什么患者走哪条线**，且两模块方案内核目前不一致（见《肌少症智能训练方案整改需求》P6）。

### 2.3 概念矩阵

| 功能 | 归属主线 | 肌少症模块 | 复用关系 |
|---|---|---|---|
| 患者登记 | 主系统 | — | 共享 |
| 综合/肌力评估 | 主系统 | 复用指标 | 共享 |
| 出方案 | `#/plan` | `#/sarcopenia` | **双引擎，需统一** |
| 设备处方 | `PlanEngine.QD_RX` | `PLAN_LIB.device`(占位) | **矛盾，需同源** |
| 动作库 | `StrengthLib` | `SarcExerciseLib` | 同库不同表 |
| 随访台账 | 报告中心 | 肌少症专项台账 | 并行 |

---

## 三、现存导航问题（导致培训成本高）

1. **双方案入口混淆**：「智能方案生成」与「肌少症专项台账」并列，新用户不知按人群选哪条。
2. **方案库编辑深埋后台**：「运动方案库管理中心」藏在「系统设置 / adminOnly」，与「出方案」页面物理隔离，管理员不易建立「我改库→方案变」的因果认知。
3. **无功能导引/新手引导**：全站没有任何「这是什么、怎么用」的常驻说明，全靠口训。
4. **资讯/消息 5 项占据首屏分组**：对绝大多数医生是无用噪声，挤占认知带宽。
5. **设备档案归类错位**：「鹊动设备档案」放在「系统设置」而非贴近「方案/设备使用」处，与「设备处方」脱节。
6. **缺少角色视图**：admin 与 doctor 看到的应是不同精简集合，但当前只是 `adminOnly/doctorOnly` 过滤，未做「角色默认工作台」。

---

## 四、整改建议（重构后 NAV 草案）

> 原则：① 按「工作流顺序」而非「技术模块」分组；② 方案入口合并为「一个智能方案」，内部按人群切换；③ 方案库管理与设备档案贴近方案；④ 资讯消息收敛为单入口；⑤ 新增「功能导引」常驻。

```js
const NAV = [
  { section: '工作台', items: [
    { hash:'#/dashboard', icon:'🏠', label:'工作台首页' },
    { hash:'#/guide',     icon:'🧭', label:'功能导引（新手必看）' }, // 新增
  ]},
  { section: '评估与测评', items: [
    { hash:'#/patient',       icon:'📝', label:'患者首诊登记' },
    { hash:'#/assessment',    icon:'📊', label:'综合评估' },
    { hash:'#/lifestyle',     icon:'🌿', label:'生活方式问卷' },
    { hash:'#/isokinetic',    icon:'⚙️', label:'等速肌力评估' },
    { hash:'#/isotonic',      icon:'🏋️', label:'等张肌力评估' },
  ]},
  { section: '智能方案（一体化）', items: [
    { hash:'#/plan', icon:'🎯', label:'智能方案生成',
      hint:'体重管理 / 老年肌少症 自动切换，含鹊动设备+居家徒手' }, // 合并原两入口
  ]},
  { section: '专病管理 · 老年肌少症', items: [
    { hash:'#/sarcopenia',       icon:'🧓', label:'肌少症专项台账' },
    { hash:'#/sarcopenia-assess',icon:'🩺', label:'肌少症标准化评估' },
    { hash:'#/sarcopenia-stats', icon:'📈', label:'肌少症数据看板' },
  ]},
  { section: '设备与方案库', items: [
    { hash:'#/devices',         icon:'🔧', label:'鹊动设备档案' },
    { hash:'#/action-library',  icon:'🃏', label:'运动方案库管理', adminOnly:true },
  ]},
  { section: '报告与数据', items: [
    { hash:'#/report',   icon:'📄', label:'报告管理中心' },
    { hash:'#/bigdata',  icon:'🚀', label:'大数据展示' },
  ]},
  { section: '资讯与消息', items: [
    { hash:'#/info-center', icon:'📚', label:'资讯中心', doctorOnly:true },
    { hash:'#/msg-center',  icon:'💬', label:'系统消息',  doctorOnly:true },
    { hash:'#/info-admin',  icon:'📰', label:'资讯管理',  adminOnly:true },
    { hash:'#/msg-admin',   icon:'🔔', label:'消息管理',  adminOnly:true },
    { hash:'#/info-groups', icon:'👥', label:'接收人分组', adminOnly:true },
  ]},
  { section: '系统设置', items: [
    { hash:'#/admin', icon:'⚡', label:'系统管理后台', adminOnly:true },
  ]},
];
```

**关键变化说明**
- **新增「功能导引」常驻入口**（见第五节）：把培训成本从「人对人」转移到「系统内置」。
- **「智能方案生成」合并双入口**：内部按患者标签（年龄≥60 且 SARC-F 阳性 → 自动切肌少症视图；否则体重管理视图），医生无需在两条线间纠结。
- **设备档案 + 方案库管理上提到「设备与方案库」分组**：与出方案紧邻，强化「改库即改方案」认知。
- **资讯/消息收敛为单分组且置于后部**，doctor/admin 各自只看到 2 项。
- **「系统设置」仅留后台**，降低日常噪声。

---

## 五、功能导引落地建议（真正降低培训成本）

1. **首次进入引导浮层（Onboarding）**：用户首次登录按角色弹出 3–4 步导览（登记→评估→出方案→看报告），可跳过、可「不再提示」。
2. **每页一句话说明**：每个路由页顶部加一行 `subtitle`（如「智能方案生成：基于您的评估数据，自动匹配鹊动设备与居家徒手训练」），hover 可展开详情。
3. **流程串联提示**：在「综合评估」完成页提示「→ 去生成智能方案」；在方案页提示「→ 去报告中心导出」。
4. **小Qoo 宠物做引导员（已具备 `window.QooPet.say()`）**：首次进入关键页时小Qoo 冒泡一句指引；点击可重复播报。把已有宠物能力接入导引。
5. **角色默认首页**：admin 默认进「系统管理后台概览」，doctor 默认进「工作台」，减少无关入口曝光。

---

## 六、实施优先级（建议）

| 优先级 | 动作 | 工作量 | 降本效果 |
|---|---|---|---|
| P0 | 新增「功能导引」入口 + 首页 subtitle | 小 | 高 |
| P0 | 「智能方案生成」合并双方案入口（内部切换） | 中 | 高（消除最大混淆） |
| P1 | 设备档案 + 方案库管理上提分组 | 小 | 中 |
| P1 | 资讯/消息分组后置 + 角色视图 | 小 | 中 |
| P2 | 首次进入 Onboarding 浮层 | 中 | 高 |
| P2 | 小Qoo 接入页面导引 | 小 | 中 |

> 备注：导航整改**纯前端、零风险**，可独立于方案内核整改先行落地；与《肌少症智能训练方案整改需求》并行不冲突。
