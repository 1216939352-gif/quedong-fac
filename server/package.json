{
  "name": "quedong-backend",
  "version": "0.1.0",
  "private": true,
  "description": "鹊动FAC功能评估与干预系统 — 局域网后端骨架（Node + Express + 内置 node:sqlite）",
  "main": "server.js",
  "scripts": {
    "start": "node --experimental-sqlite server.js",
    "backup": "node --experimental-sqlite backup.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.2.0"
  },
  "engines": {
    "node": ">=22.5"
  }
}
