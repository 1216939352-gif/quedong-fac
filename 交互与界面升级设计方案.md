# 鹊动系统 · 交互与界面升级设计方案

> 目标：在现有「液态玻璃」设计语言基础上，让**操作流程更顺、页面信息更透、看板/统计更可下钻、可编辑页更聪明**。
> 范围：大数据看板（`modules/bigdata.js`）、肌少症统计（`modules/sarcopenia.js → Pages.sarcopeniaStats`）、综合评估/方案/报告等可编辑页（`assessment.js` / `plan.js` / `report.js` / `sarcopenia.js` 向导）。
> 约束：保持静态 SPA + 全局 `Pages` 渲染架构不变；新增交互全部复用现有 `U.el / U.toast / U.confirm` 与 CSS 变量体系。

---

## 一、现状诊断（基于代码走查）

| 模块 | 现状 | 主要痛点 |
|---|---|---|
| **大数据看板** | 8 个 KPI 卡 + 8 个图表卡（性别/年龄/BMI/风险/生活方式/趋势/医生TOP5/方案类型）+ 漏斗；图表为静态 SVG，`donutChart/barChart/lineChart/hbarChart` 已封装 | ① 信息平铺、**无下钻**；② 卡片**不可展开**；③ 图表仅 donut 有 `<title>` hover，无排序/筛选/时间维度；④ 缺指标解读与参考区间；⑤ 无异常洞察聚合 |
| **肌少症统计** | 4 KPI + 3 donut（方向/跌倒风险/分级）+ 漏斗 + 明细表（min-width 940 横向滚动）+ CSV/JSON 导出 | ① 同上，图表不可展开；② 明细表**列多、移动端必须横滑**；③ 每行**不能展开看完整评估结论**；④ 无按方向/风险/分级/时间筛选 |
| **可编辑页** | `assessment.js` 等已用 `.card` + `calc-trigger` 即时计算；9 步向导进度可见 | ① 长表单无渐进披露；② 缺智能默认值/单位提示；③ 字段级校验弱（无超范围即时纠正）；④ 无自动保存可见状态；⑤ 向导可跳步但校验不锁步；⑥ 上传解析后回填无高亮"演示" |

**一致性缺口**：看板用 `skin` 橙色系 + 光球背景，统计页复用 `bigdata-*` 类但缺光球氛围；可编辑页用 `.card` 实心白卡，与看板的玻璃语言割裂。

---

## 二、设计原则（四条）

1. **分层与渐进披露**：默认给概览，深度按需展开（看板卡可展开、明细表主行/子行、长表单分节折叠）。
2. **状态可见、操作有反馈**：乐观更新、自动保存指示、加载/空/错误态齐备；出错**保留输入**仅高亮。
3. **统一设计语言**：玻璃质感 + 橙(#f26522)/青(#00b4d8) 双色贯穿看板与表单；统一卡片、芯片、按钮、动效曲线。
4. **数据叙事**：每张图配 ≤1 行解读 + 参考区间；系统在顶部聚合"异常洞察条"。

---

## 三、大数据看板重设计

- **顶部控制条**：机构名 + 时间范围切换（今日 / 7天 / 30天 / 自定义）+ 演示数据开关。切换后仅重算当前页，URL hash 记忆筛选。
- **KPI 卡 → 可展开卡**：默认显示数值 + 环比；点击 ▸ 展开 → 显示构成明细（mini 列表/小柱图）+ 一句话解读 + "导出该卡"按钮。
- **图表卡 → 可下钻卡**：标题右侧 ▸ 展开箭头；展开进入下钻视图（例：BMI 分布展开 → 按性别堆叠 + 高风险占比解读 + 维度筛选 chips）。
- **洞察条（Insight Rail）**：根据数据自动生成 2–4 条异常提醒（如"中心性肥胖 32 人占 28%，建议增加有氧处方"），可点击定位到对应卡。
- **每张图配数据叙事文案**（≤1 行），例如年龄分布卡："60–69 岁占比最高，为干预核心人群"。

实现要点：`bigdata.js` 现有图表函数可直接复用，只需为每个 `bigdata-card` 包一层 `data-expandable` + 展开面板 DOM；新增 `insightRules(s)` 在聚合后跑一遍。

---

## 四、肌少症统计页重设计

- **KPI 卡可展开**：如"跌倒高风险占比"展开 → 列出高风险成员名单（卡片式，移动端友好）+ 导出。
- **三张 donut 可点击下钻**：点某区段 → 下方明细表自动按该维度筛选，并展开该区段成员列表。
- **明细表 → 主行 + 可展开子行**：主行仅 4 列（编号 / 姓名 / 分级 chip / 风险）；点 ▸ 展开子行显示**完整评估结论**（分级依据、小腿围/握力/步速/SMI/体脂、干预方向、建议复查、查看报告链接）。
- **筛选栏**：干预方向 / 跌倒风险 / 分级 / 时间 组合过滤；筛选态写入 hash 便于分享/复现。
- **移动端**：宽表自动转为"可展开卡片列表"，不再横滑。

---

## 五、可编辑页交互优化（通用规范）

- **渐进式披露**：长表单按逻辑分节，节可折叠并显示"完成度"（如 3/6 已填）；当前节高亮。
- **智能默认值 + 单位提示**：常见字段带占位示例与单位后缀；性别/年龄由首诊带出。
- **实时范围校验**：超范围字段红色描边 + 气泡提示，**保留输入**不清空；通过 `input` 事件即时反馈。
- **自动保存**：草稿每 3s 或失焦保存，顶部显示"已保存 HH:MM"；刷新/跨页不丢。
- **即时计算升级**：`calc-trigger` 结果升级为内联结果卡 + 解读（如 BMI 24.1 → "超重临界"）。
- **分步导航**：9 步向导显示进度胶囊，可跳回已完成步；未通过当前步校验则锁下一步并提示。
- **上传/解析可视化**：OCR 原文可见、回填字段**高亮闪烁**、支持逐项确认/手动覆盖。
- **无障碍**：焦点态可见、触摸目标 ≥44px、尊重 `prefers-reduced-motion`。

---

## 六、落地优先级（建议）

| 阶段 | 内容 | 涉及文件 |
|---|---|---|
| **P0** | 看板/统计"可展开卡 + 下钻"骨架 + 洞察条 + 数据叙事 | `bigdata.js`、`sarcopenia.js`(stats) |
| **P1** | 明细表主行/子行 + 筛选栏 + hash 记忆 | `sarcopenia.js`(stats) |
| **P2** | 可编辑页自动保存 / 字段校验 / 渐进披露 / 向导锁步 | `assessment.js`、`sarcopenia.js`(向导)、`plan.js`、`report.js` |
| **P3** | 移动端卡片化、玻璃语言统一、动效打磨 | `styles.css` 全量 |

---

## 七、下一步

上述方案确认后，我可**按 P0 → P3 顺序逐阶段实现**，每阶段交付可运行页面并经 `node --check` 校验、同步 `_dl3`、重新部署。
建议先从 **P0（看板与统计的可展开/下钻）** 入手——它最能直接回应"信息更丰富、每项可独立展开"的核心诉求，且改动集中在两个文件、风险可控。
