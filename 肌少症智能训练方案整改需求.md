# 老年肌少症模块 · 智能训练方案逻辑整改需求

> 文档性质：现状审计 + 整改需求规格（仅需求，不含代码改动）
> 审计对象：`lib/sarc-exercise-lib.js`（`SarcExerciseLib.match` 36 动作库匹配算法）、`modules/sarcopenia-core.js`（`PLAN_LIB` / `buildPlan` / `decideDirection`）、`modules/sarcopenia.js`（报告渲染）
> 参照基准：主线 `modules/plan-engine.js`（`PlanEngine`，已上线的「严谨版」范式）

---

## 一、现状逻辑概述

肌少症模块的「智能训练方案」由三层拼装而成：

1. **方向决策 `decideDirection`**（`sarcopenia-core.js`）：依据握力/肌量/体脂/ SARC-F 等算出 `key` ∈ {`maintain`, `gain`, `lose`, `both`}，并独立算出 `sarcGrade`（严重/确诊/前期/可能/未见）。
2. **方案库 `PLAN_LIB` + `buildPlan`**（`sarcopenia-core.js`）：按 `key` 取一份**静态模板**（居家 `home` / 设备 `device` 两套纯文本动作清单 + 频次强度文案 + 饮食/生活方式）。
3. **动作匹配 `SarcExerciseLib.match(ctx)`**（`lib/sarc-exercise-lib.js`）：真正的「36 动作库算法」，输出 `exercisePlan`（热身/主训/防跌倒/有氧/放松 五组，含 recommend/optional/forbidden 状态）。按 `gradeKey`(A/B/C/D，由 `key` 映射) + 跌倒分层 + SPPB 分级 + 若干客观指标做门禁与微调。

渲染层（`sarcopenia.js`）把三者拼成报告，并标注「🏠 居家徒手智能匹配方案（36 动作库·算法自动生成）」。

**结论：动作匹配算法本身有结构（SPPB 姿态门禁、跌倒分层强制干预、客观指标微调），比完全模板化的 `PLAN_LIB` 更合理；但整体仍不满足「严谨、可落地、可解释、与客观数据强绑定」的要求，且与主线 `PlanEngine` 范式严重不一致。**

---

## 二、现有逻辑审计（问题清单）

### P1 · 干预分级与肌少症严重程度脱钩（关键缺陷）
- `gradeKey` 由 `direction.key`（体重管理方向：维持/增肌/减脂/同调）映射为 A/B/C/D，**并非由肌少症严重程度 `sarcGrade` 驱动**。
- 后果：一名「严重肌少症（三项阳性）但体脂正常」的老人 → 方向 `gain` → grade B（单纯增肌）；一名「肌少症前期」老人若同时偏胖 → 方向 `both` → grade D（肌少性肥胖）。
- **严重程度（`sarcGrade`）在 `decideDirection` 已算出，却从未传入 `buildPlan` / `match` 去塑形剂量与动作**，导致重度患者拿到的方案与轻度患者只有动作清单差异、无剂量/风险降级。

### P2 · 强度/剂量无客观数据锚定（与主线同源缺陷）
- `PLAN_LIB[dir].home.intensity` 文案写「系统根据握力、SMI 指数、SPPB 得分自动匹配起始负荷」「系统自动匹配老人最低起步负荷」——**但代码里没有任何依据握力/1RM/SMI 计算负荷/组数/次数的实现**。
- `match` 输出只有 `freq`/`duration`/`safety` 文案，**没有逐动作剂量（sets×reps×%1RM 或 RPE）**。每个动作的 `params` 是库里写死的静态值，不随评估变化。
- 即系统对外承诺了「量化自适应负荷」，实际未兑现。

### P3 · 动作库 ID 硬编码、与可编辑方案库脆弱耦合
- `match` 大量使用 `idsOf([19,20,22,…])`、`status['H21']` 等**数字/字符串混用 ID 硬编码**。
- 管理员在「运动方案库管理中心」增删改动作后，匹配器的硬编码 ID 立即失效或推荐到错误/不存在的动作，且无任何校验告警。

### P4 · SPPB 仅做「姿态门禁」，未动态调节剂量
- SPPB 0–5 仅屏蔽站立动作、文案加「单次≤15 分钟」；SPPB 6–9 屏蔽无扶手站立进阶。
- 但**组数、次数、强度上限（RPE 天花板）未按功能衰退程度递减**，重度衰弱者仍拿到与常人相同的动作参数。

### P5 · 缺失 LSI / 弱侧单侧处理
- 主线 `PlanEngine` 已支持肢体对称指数（LSI）弱侧单侧处方；肌少症 `match` **完全没有左右不对称、偏瘫/术后单侧肌萎的处理逻辑**——而单侧肌少在老年/卒中后极常见。

### P6 · 设备处方仍是占位文本，未接入设备库与客观数据
- `PLAN_LIB[dir].device.actions` = `['鹊动下肢屈伸训练设备', …]`，是**自由文本设备名**，未映射到 `CONST.DEVICES`（9 台真实设备）与 `PlanEngine.QD_RX`（含客观配速配阻的处方单）。
- 结果：设备方案无法量化执行，且与主线严谨设备处方形成**两套互相矛盾的设备方案体系**。

### P7 · 禁忌/禁忌症未真正驱动方案
- `computeAll` 已算出 `jointIssue`（关节疾病）、`fallHistory` 等，但 `buildPlan` 的 `ctx` **未把 `jointIssue` 传给 `SarcExerciseLib.match`**。
- 后果：存在膝关节疾病的患者，方案仍可能推荐「靠墙静蹲/站姿抬踵」等加重受累关节的动作；急性禁忌（`CONTRA_ITEMS`）只在评估准入环节拦截，**未反向从动作库剔除冲突动作**。

### P8 · 无退阶→基础→进阶周期化
- 动作虽有 `levelLabel`（初级/进阶），但方案是**扁平清单**，没有「第 1–2 周退阶 → 第 3–4 周基础 → 进阶」的渐进阶梯，长期可执行性差。

### P9 · home / device 双方案割裂，无统一 schema
- 居家与设备方案分别来自 `PLAN_LIB` 与 `match`，数据结构、字段、渲染各自为政，医生需自行脑补合并；无法输出一份「一体化周计划」。

### P10 · 配图缺失
- 动作卡仅有 `figureSVG`（极小姿态占位图标），未复用本轮已生成的 5 张小Qoo 演示风插图，也无 `illustrationRef`/`registerImage` 钩子，方案「不生动」。

### P11 · 可解释性弱
- `match` 返回 `flags` 但**没有 `reasons` 数组说明「为什么推荐/屏蔽某动作」**。医生无法审计算法依据，存在临床信任与合规风险（主线 `PlanEngine.summary.reasons` 已解决此问题）。

### P12 · 报告文案过度承诺
- 报告标注「36 动作库·算法自动生成」暗示高度个性化，实际是「分级 + 跌倒 + SPPB 门禁 + 轻微微调」，个性化剂量有限。措辞需收敛为「分级匹配方案（基于 SPPB/跌倒/客观指标）」。

---

## 三、整改目标

对齐主线 `PlanEngine` 已验证的范式，使肌少症方案达到：

1. **严重度感知**：`sarcGrade` + 身体成分方向共同驱动分级与剂量。
2. **客观锚定**：握力、等张 1RM（经 Brzycki 反推）、SMI、SPPB、跌倒指数 → 逐动作 sets×reps×强度（%1RM 或 RPE 上限）。
3. **禁忌网关优先**：急性禁忌→转介；关节/疾病禁忌→动作级屏蔽 + 说明。
4. **弱侧单侧**：支持 LSI 与偏瘫侧处方。
5. **设备同源**：设备处方接入 `CONST.DEVICES` + `QD_RX`，与主线一致。
6. **统一 schema**：home/device/exercise 同源输出一份周计划。
7. **周期化**：退阶→基础→进阶 4 段可落地。
8. **配图生动**：复用小Qoo 演示风插图钩子。
9. **可解释**：每条动作附 `reasons`。
10. **文案诚实**：去除夸大表述。

---

## 四、整改需求清单（可执行规格）

| 编号 | 需求 | 关键规格 | 关联问题 |
|---|---|---|---|
| R1 | 严重度感知分级 | `buildPlan` 接收 `sarcGrade`，定义新的分级维度 `severity ∈ {前期, 确诊, 严重}`，与 `direction` 正交组合成剂量矩阵 | P1 |
| R2 | 客观数据锚定剂量 | 新增 `anchorDose(ctx)`：握力→握力动作 RPE；1RM→等张动作 %1RM（Brzycki 反推）；SMI/SPPB→抗阻容量上限；输出 `sets/reps/intensity` 写入每个动作 | P2 |
| R3 | 禁忌强制网关 | `match` 入参增加 `contra`(急性) / `jointIssue` / `acuteFlags`；急性→整体转介文案；关节禁忌→按 `tags` 屏蔽对应关节动作并记 `reasons` | P7 |
| R4 | LSI 弱侧单侧 | 入参增加 `lsi`/偏瘫侧；弱侧单侧额外处方，标注「患侧/健侧」 | P5 |
| R5 | 设备处方同源 | 设备动作从 `CONST.DEVICES`+`QD_RX` 取客观处方，强度锚定等速/等张数据；无客观数据时走「宣教+待校准」双轨（复用主线逻辑） | P6 |
| R6 | 统一方案 schema | 定义 `SarcPlan = { meta, safety, home, device, exercise, weekly, reasons }`，`home/device/exercise` 同源于 `anchorDose` | P9 |
| R7 | 周期化阶梯 | `weekly` = 退阶(W1-2)/基础(W3-4)/进阶(W5+) 三阶段，每阶段 sets×reps 递增、强度上限受 `sarcGrade` 约束 | P8 |
| R8 | 配图钩子 | 每个动作带 `illustrationRef`；`registerImage()` 复用已生成的 qd-01/bw-squat-2/bw-push-2 等 | P10 |
| R9 | 可解释 reasons | 每条动作记录 `reasons:[{factor, detail}]`（如「SPPB=4 仅开放坐姿」「膝关节禁忌屏蔽站姿」） | P11 |
| R10 | 文案收敛 | 报告标注改为「分级匹配居家方案（基于 SPPB/跌倒风险/客观肌力）」；删除「系统自动匹配负荷」等未实现承诺 | P12 |
| R11 | 动作库解耦 | `match` 按 `cat`/`tags`/`level` + 准入规则匹配，**废弃硬编码 ID**；管理员改库后匹配器自洽 | P3 |
| R12 | SPPB 剂量联动 | SPPB 0–5 → RPE≤10、组间休息延长、单组次数下调；6–9 → RPE≤11；10–12 → 常规 | P4 |

---

## 五、数据依赖清单

| 数据 | 来源 | 是否已具备 |
|---|---|---|
| 握力 (gripEval.value/low) | 综合评估/肌少症测评 | ✅ |
| 等张 1RM (isotonicData.oneRML/R) | 等张测评（OCR 已修复） | ✅（需测评） |
| SMI / 肌量 (body.smiLow) | 体成分 | ✅ |
| SPPB 总分 | 步态/平衡/椅子起立 | ✅ |
| 跌倒指数 (fallRiskIndex) | 综合计算 | ✅ |
| 关节禁忌 (jointIssue) | 生活问卷 | ⚠️ 已算未传参 |
| 急性禁忌 (CONTRA_ITEMS) | 评估准入 | ✅ 未反向驱动 |
| LSI / 偏瘫侧 | 等速左右对比 | ⚠️ 主线已有，肌少症未接 |

---

## 六、验收标准

1. 严重肌少症（三项阳性）患者：方案剂量显著低于肌少症前期（RPE 上限、组数递减可核验）。
2. 膝关节禁忌患者：报告不出现任何加重膝关节的站姿/蹲类动作，且 `reasons` 注明屏蔽依据。
3. 偏瘫患者：出现「患侧/健侧」区分处方。
4. 有等张 1RM 患者：抗阻动作强度显示为「X% 1RM」，无数据则显示「宣教+待校准」。
5. 管理员在方案库增删动作后，匹配器不报错、不推荐不存在动作。
6. 报告文案无「自动匹配负荷」等未实现承诺。
7. 每条推荐动作可点开查看 `reasons`。

---

## 七、与主线 PlanEngine 的分工建议

- **推荐方案：双轨并存、内核统一**。肌少症是「专病闭环」（独立台账/评估/报告/随访），保留其独立入口；但其方案生成内核应**复用** `PlanEngine` 的 `anchorDose` / 禁忌网关 / LSI / 设备处方 / 配图钩子，避免维护两套矛盾逻辑。
- 即：`SarcEngine.buildPlan` 内部调用 `PlanEngine` 的剂量与设备处方子模块，仅在外层叠加肌少症专属的「严重度分级 + 防跌倒专项 + 营养联动」。
- 这样新增一个动作/设备，只需在一处维护内容库，两模块同时受益。
