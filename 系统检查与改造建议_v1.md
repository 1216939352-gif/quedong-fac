# 系统功能与业务流程整体检查 · 改造建议（v1）

> 范围：鹊动FAC / 肌少症 / 跌倒风险 三方向系统（经典 `<script>` SPA：`_dl3/` 前端 + `server/` 后端）。
> 依据：实际代码定位（app.js / modules/*.js / server/ai-*.js / styles.override.css）。

---

## 一、现状盘点（已核实）

### 1. 导航结构（app.js:719-787，NAV 数组）
| 分组 | 条目 |
|---|---|
| 工作台 | 工作台首页、功能导引 |
| 患者登记(step1) | 患者首诊登记 |
| 综合评估中心(step2) | 体重管理评估、肌少症标准化评估、跌倒风险评估、生活方式问卷、等速肌力、等张肌力 |
| 制定方案(step3) | 智能方案生成 |
| 报告与数据(step4) | 报告管理中心、**大数据展示** |
| 专病管理·老年肌少症 | 肌少症专项台账、肌少症数据看板 |
| 设备与方案库 | 鹊动设备档案、运动方案库管理 |
| 资讯与消息 | 资讯中心/系统消息（医生/管理员） |
| 系统设置 | 系统管理后台 |

### 2. 路由（app.js:950-978，ROUTES）
- 体重管理看板：`#/bigdata` → `Pages.bigdata()`（modules/bigdata.js，仅体重维度）
- 肌少症看板：`#/sarcopenia-stats` → `Pages.sarcopeniaStats()`（modules/sarcopenia.js:2840）
- **跌倒风险无独立看板路由**

### 3. 看板实现
- `modules/bigdata.js`：体重管理健康看板，含 KPI/环形/柱/折线/横向条形/漏斗 + 展开下钻 + 智能洞察条 + 时间范围 + CSV 导出。其 `calcStats()` 只读体重数据（BMI/腰围/生活方式/等速/方案）。
- 根目录 `bigdata.js`：**重复定义 `Pages.bigdata`**，因 modules 版后加载而覆盖，属死代码，应清理。
- 肌少症看板：`sarcopeniaStats()`，结构独立、与体重看板风格不一。

### 4. 功能导引（app.js:981-1021，Pages.guide）
4 步旧流程（登记评估 / 生成方案 / 设备方案库 / 输出运营），**无独立跌倒风险条目**，与三方向现状脱节。

### 5. AI 模块
- 后端 `server/ai-config.js`：每提供方（cloud/local）仅 1 个 model；`resolveProvider(prefer)` 已支持 local/cloud/hybrid 偏好。
- 后端 `server/ai-routes.js`：`chatOnce`(292) → `callCloud`(56)/`callLocal`；`/chat`(591)、`/interpret`(776)、`/generate-plan`(677)、SSE 版。均接收 `b.prefer`，但**不接收 model**。
- 前端 `modules/ai-reason.js`：`aiControls`(1093)、`openChat`(957，浮窗 `#ai-chat-text` 输入框)、`chat/interpret/generatePlan` 均接受 `opts` 但只透传 `prefer`。**无模型选择 UI、无语音输入**。

---

## 二、主要问题（按影响排序）

1. **三方向在导航中不同级、不完整** — 体重管理只有"评估入口"无看板/台账对等项；肌少症出现两处；跌倒风险无任何后续（看板/台账）入口。用户无法从导航感知"三个维度各有完整闭环"。
2. **看板维度缺失** — 只有体重 + 肌少症两个看板，**缺跌倒风险看板**；且现有大盘只聚合体重数据，无法看三方向联合视图。
3. **功能导引过时** — 不反映三方向与三看板，新用户/新同事培训成本未降低。
4. **重复/死代码** — 根目录 `bigdata.js` 与 `modules/bigdata.js` 重复定义，易引发维护歧义。
5. **AI 能力待升级** — 单模型、无模型切换、无语音输入（满足"AI 升级计划"诉求）。

---

## 三、改造建议

### A. 导航重组（三方向同级 + 各自闭环）
建议新增「数据看板中心」作为统一看板枢纽，使三个方向各自拥有"评估 → 方案 → 看板"对等入口：

```
工作台        ：工作台首页、功能导引
患者登记      ：患者首诊登记
综合评估中心  ：体重管理评估 / 肌少症标准化评估 / 跌倒风险评估 / 生活方式问卷 / 等速肌力 / 等张肌力
智能方案      ：智能方案生成
数据看板中心  ：体重管理看板(#/bigdata) / 肌少症看板(#/sarcopenia-stats) / 跌倒风险看板(#/fall-risk-stats)
专病管理      ：肌少症专项台账(#/sarcopenia) / 跌倒风险台账(复用报告中心"方向"筛选)
设备与方案库  ：鹊动设备档案 / 运动方案库
资讯与消息    ：资讯中心 / 系统消息 / 资讯管理 / 消息管理
系统设置      ：系统管理后台
```
> 看板"3 个独立页 vs 肌少症+跌倒风险合并为一个入口(两页/明细区分)"见下方第④决策点。

### B. 功能导引重写（对齐三方向+三看板）
四组卡片改为：① 登记与评估（含跌倒风险评估卡）② 方案与干预（智能方案生成 + 各方向方案）③ **数据看板（三看板卡，新增）** ④ 管理与运营。内置"任务流程"渲染继续复用 `renderWorkflow()`。

### C. 看板重构
1. **清理死代码**：删除根目录 `bigdata.js`（保留 `modules/bigdata.js`）。
2. **抽公共图表内核**：将 `donutChart/barChart/lineChart/hbarChart/legendHTML/animateBigdata` 抽到 `modules/dashboard-core.js`，三看板页面薄封装，避免三套重复实现。
3. **新增跌倒风险看板** `Pages.fallRiskStats`(#/fall-risk-stats)：数据来自 `FallDB().listRecords()`，展示
   - 风险等级分布（高/中/低，环形图）
   - 高危因子频次（用药≥4种 / 单腿站立<5s / TUG>12s / 步速<0.8 / FES-I≥19 / 居家环境≥3项，横向条形）
   - 年龄分布、FES-I 分布、TUG/BBS 均值
   - 近 30 天评估趋势（折线）、转诊建议命中数
   - 复用现有图表内核 + 展开下钻 + 洞察条 + CSV 导出
4. **可选**：在「数据看板中心」首页做聚合视图（三方向关键指标一屏总览），每个指标钻取进对应看板。

### D. 数据层提示
三方向存储范式不同（体重=`AppState.patients`、肌少症=`SarcDB`、跌倒=`AppState.patient.data.fallRecords`）。看板聚合查询需分别取数；建议抽一个 `getDirectionStats(dir)` 统一适配层，看板只调此层，降低耦合。（Phase 3 独立表重构前，此为最小改动方案。）

---

## 四、需你拍板的决策点（执行前确认）
- **④ 看板架构**：三独立看板 / 肌少症+跌倒风险合并入口(两页) / 看板中心聚合页 + 钻取
- **⑤ 实施顺序**：先导航+导引(A/B) → 看板(C) → 还是先做 AI 升级
- 其余（清理死代码、抽图表内核、数据适配层）为明确收益项，建议一并执行。

> 详细 AI 升级（多模型 + 语音输入）实现路径见《AI升级实现路径_v1.md》。
