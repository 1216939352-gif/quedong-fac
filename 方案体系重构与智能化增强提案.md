# 鹊动FAC功能评估与干预系统 · 方案体系重构与智能化增强提案（V1 方案稿）

> 本文为**方案设计稿**，不含任何代码改动。目标是基于现有 SPA（已有全局 `AppState` / `DB` / `SarcDB` / `Pages` / `CONST.DEVICES` 等）之上，重构方案匹配与内容，并新增「配图自动生成 / 内置 AI / 小Qoo 宠物」三大能力。

---

## 0. 问题诊断与总目标

### 0.1 现状问题（来自你的反馈）
- 老年体重管理、肌少症运动方案、体重管理智能方案「比较粗糙」：生成逻辑偏模板化，与评估结果的关联弱。
- 徒手方案、鹊动方案「看似丰富但不够严谨、可落地性不强」：缺强度锚定、缺退阶/进阶、缺动作要点与禁忌、缺配图。

### 0.2 总目标
1. **匹配逻辑可解释**：规则引擎驱动，非黑箱。
2. **方案内容严谨可执行**：每次训练都有目标 / 组次 / 强度 / 要点 / 退阶进阶 / 禁忌。
3. **鹊动设备为核心处方**：用等速 / 等张客观数据驱动强度。
4. **居家徒手方案规整**：按动作模式分类 + 周期化进阶。
5. **生动配图自动生成**：统一「小Qoo 演示风」。
6. **内置 AI**：评估 → 方案 + 问答。
7. **小Qoo 跨页可拖动宠物**：点击互动。

---

## 1. 方案匹配逻辑重构（规则引擎）

### 1.1 数据底盘（输入维度）
从现有评估可直接取用的字段：

| 维度 | 字段（现有系统） |
|---|---|
| 基础 | 性别、年龄、身高、体重、BMI、体脂率 |
| 肌少症五项 | 握力 `grip`（男<27 / 女<18）、4m 步速 `gait`（<0.8）、SPPB（步速/平衡/座椅）、小腿围 `calf`、肌少症筛查 `sarcf` |
| 设备肌力 | 等速峰力矩/功/功率（`strength.total` 0-100）、等张 1RM `oneRML/R`、1RM/BW `rm1BwL/R`、肢体对称指数 `LSI` |
| 营养 | MNA-SF `mnasf`、目标热量 `nutrition.target` |
| 风险 | `risk.level`（high/mid/low）、`contraindications`（合并症/禁忌） |
| 环境 | 是否拥有鹊动设备、居家空间、训练经验 |

### 1.2 匹配决策模型（自顶向下，安全闸门优先）
1. **安全闸门（最高优先）**：存在禁忌证 / 急性期 → 仅给「医疗转介 + 安全宣教」，不出训练处方。
2. **人群分层**：老年（≥65 或肌少症风险阳性）/ 成人。
3. **目标分型**：减脂 / 增肌 / 肌少症预防 / 肌少症逆转 / 功能维持 / 康复。
4. **设备可及**：有鹊动设备 → 设备处方为主 + 徒手为辅；无设备 → 徒手为主 + 弹力带渐进。
5. **强度锚定**：用客观数据推导 %强度（见 1.3），而非写死。
6. **容量编排**：频率/周期由 `risk.level` + 阶段 `phase` 决定（现有 `weekSchedule(risk, phase, 2, 2)` 可扩展）。

**匹配矩阵（人群 × 目标 × 设备 → 方案构成）**

| 人群 | 目标 | 设备 | 方案构成（设备:徒手:有氧 次/周） | 强度锚定 |
|---|---|---|---|---|
| 老年+肌少症风险 | 逆转/预防 | 有 | 3 : 2 : 2 | 设备=等张 1RM 60-70% / 等速 50%峰值；徒手 RPE 11-13 |
| 老年+肌少症风险 | 逆转/预防 | 无 | 0 : 3 : 2 | 徒手 RPE 11-13 + 弹性带渐进 |
| 成人超重 | 减脂 | 有 | 2 : 2 : 3 | 设备 1RM 70-80%；有氧中强度 |
| 成人超重 | 增肌 | 有 | 3 : 1 : 1 | 设备 1RM 75-85% |
| 成人 | 功能维持 | 无 | 0 : 3 : 2 | RPE 12-14 |
| 高危/急性期 | 任何 | — | 0 : 0 : 0（仅宣教+转介） | — |

### 1.3 强度锚定规则（严谨关键）
- **等张肌力**：负荷 = 1RM × 目标%。例：1RM=21kg，增肌 80% → 负荷≈17kg（来自 `isotonicData.oneRML/R`）。
- **等速肌力**：以峰力矩 % 设定（等速更安全，50% 峰值力矩做耐力、70% 做力量）。
- **徒手**：用 RPE（Borg 6-20）或「可标准完成次数」反推；老年用 RPE 11-13。
- **LSI（肢体对称指数）**：若 <85%，弱侧优先、双侧分设强度。
- **疲劳指数**：等速疲劳指数高 → 降容量。

### 1.4 输出结构（方案对象 schema，建议扩展现有 `plan`）
现有 `plan` 已有 `aerobic.phases` / `resistance.phase` / `nutrition.target` / `schedule`。建议新增：

```
plan = {
  meta:       { goalType, population, hasDevice, generatedBy:'rule'|'ai'|'hybrid', version },
  safety:     { gate, contraindications[], cautions[], redFlags[] },
  qudong:     [ { deviceId, target, sets, reps, intensityPct, restSec, rationale } ],
  bodyweight: [ { pattern, name, sets, reps, rpe, regression, progression, cues[], errors[] } ],
  aerobic:    { phases, currentPhase, weeklyMin },
  periodization: { weeks:4, progressionRules[] },
  illustrationRefs: [ { key, assetId } ]
}
```

---

## 2. 方案内容构成（严谨可执行）

### 2.1 鹊动设备处方库（核心，9 设备映射）

| 设备 | 靶肌群 | 动作模式 | 处方要点（来自测试） |
|---|---|---|---|
| 01 伸膝 | 股四头肌 | 膝伸展 | 1RM 锚定伸膝力量；膝骨关节炎者限 ROM |
| 02 屈膝 | 腘绳肌 | 膝屈曲 | 与 01 拮抗；腘绳肌拉伤史降强度 |
| 03 腹屈 | 腹直肌/核心 | 躯干屈曲 | 腰椎病免负重屈曲 |
| 04 背伸 | 竖脊肌/背阔 | 躯干伸展 | 与 03 拮抗；骨质疏松限负荷 |
| 05 胸推 | 胸大肌/三头 | 水平推 | 1RM 锚定；肩峰下撞击限肩外展 |
| 06 划船 | 背阔/肱二 | 水平拉 | 与 05 拮抗；圆肩体态优先 |
| 07 下压 | 肱三头/背阔 | 推（下） | 肘部不适降负荷 |
| 08 高拉 | 背阔/二头 | 拉（上） | 肩活动受限限 ROM |
| 09 蹬踏 | 股四/臀/腘 | 下肢蹬伸 | 多关节；心血管慎用高负荷 |

**单个设备处方单（以 01 伸膝为例，严谨字段）**
- 目标肌群：股四头肌
- 测试依据：等张 1RM-L=21kg / 等速 60°/s 峰力矩=X N·m
- 处方：3 组 × 10 次，强度 70%1RM≈15kg，组间休 90s
- 进阶：每 2 周 +5% 或 +2 次，至 80%
- 退阶：疼痛 → 降 ROM / 降 10% 负荷
- 要点：坐稳、核心收紧、匀速
- 禁忌：急性膝损伤、显著肿胀

### 2.2 居家徒手方案库（规整，按动作模式）
六类动作模式，每类给「基础→进阶→退阶」三级，保证可执行：

| 模式 | 退阶 | 基础 | 进阶 |
|---|---|---|---|
| 推 | 墙面推 | 跪姿俯卧撑 | 标准俯卧撑 |
| 拉 | 门框划船 | 毛巾水平拉 | 弹力带划船 |
| 蹲 | 扶椅坐站 | 半蹲 | 徒手深蹲 |
| 铰 | 臀桥 | 单腿臀桥 | 自重摆荡 |
| 核心 | 死虫 | 鸟狗 | 平板支撑 |
| 步态/平衡 | 脚跟脚尖走 | 单脚站 | 太极步 |

**单动作字段（与设备处方对齐）**：名称 / 模式 / 靶肌 / 组次 / RPE / 退阶 / 进阶 / 要点 / 常见错误。
例（扶椅坐站）：靶肌=股四/臀；3×8-10，RPE 11-12；退阶=加高椅面；进阶=无扶手/抱球；要点=坐到底再起、鼻吸口呼；错误=圆背、膝盖内扣。

### 2.3 周期化模板（4 周）
- 第 1-2 周 适应：强度 60-65%1RM，RPE 11-12，容量低。
- 第 3-4 周 渐进：强度 70-80%，容量↑。
- 老年人每 2 周复测 1RM/步速，动态调整（闭环）。

### 2.4 安全与禁忌模块（独立闸门）
- 红 flag：胸痛 / 眩晕 / 关节急性肿 → 停训转介。
- 相对禁忌：骨质疏松、未控制高血压 → 限强度 / 免某些动作。
- 每条处方带 `cautions` 与 `redFlags`，前端醒目展示。

---

## 3. 生动配图自动生成

### 3.1 内容-图像管线
- **资产定义**：每个「动作 / 设备演示」是一个 asset（key = 设备 id 或 动作 id）。
- **生成（build-time 或按需）**：调用图像生成服务（如 ImageGen / 内部文生图 API），prompt 由「动作名 + 设备 + 小Qoo 演示风 + 视角 + 统一画风」构成。
- **存储**：生成后存入 IndexedDB `quedong_wm_media`（与现有媒体槽一致）或静态 `assets/`，记录 `assetId`。
- **回填**：方案渲染时按 `illustrationRefs` 取图展示。

### 3.2 视觉风格系统（一致性关键）
- 固定小Qoo 形象（圆润、友善、橙色调呼应品牌 #F2651E）。
- 统一：浅色背景、正面或 3/4 侧视角、关键关节高亮、动作轨迹箭头、无文字。
- 提示词模板：
  `可爱吉祥物小Qoo（圆润橙色生物）正在演示[动作]，使用[设备]，正面平视，浅色背景，扁平插画风，关节处有半透明高亮，动作方向用箭头标注，医疗科普插画风格，无文字。`

### 3.3 落地步骤
1. 建资产清单（9 设备 + 6 模式 ×3 级 ≈ 30+ 张）。
2. POC：先生成 3-5 张（伸膝 / 坐站 / 胸推）验证画风一致。
3. 批量生成 + 入库 + 方案绑定。
4. 运行时容许「重新生成这张图」。

### 3.4 成本与降级
- 预生成一次性成本低；运行时走缓存。
- 生成失败降级为系统内置 SVG 线条示意图。

---

## 4. 内置 AI 模型（评估 → 方案 + 问答）

### 4.1 总体架构
前端（聊天面板 / 生成按钮）→ 后端 `/api/ai/*` → 编排层（Prompt 构造 + RAG + 规则闸门）→ 模型（本地/云）→ 结构化输出 → 人工审核后写入 `plan`。

### 4.2 三种部署对比

| 方案 | 优点 | 缺点 | 适用 |
|---|---|---|---|
| 云 LLM API | 质量高、快、零运维 | 健康数据出网（隐私/合规风险）、按量计费 | 非敏感/已脱敏 |
| 本地自托管（Ollama + Qwen2.5-7B/14B 或医疗微调） | 数据不出院、无调用费 | 需 GPU/服务器、首部署成本、延迟 | 临床/隐私优先 ← 推荐 |
| 混合 | 质量+隐私平衡 | 架构复杂 | 中大型机构 |

### 4.3 推荐（本地自托管 + RAG + 规则闸门）
- **模型**：院内/本地服务器跑 Qwen2.5-7B/14B（中文强、可量化）；或接入已有合规医疗大模型。
- **后端**：复用现有 Node（`_local_server.js` / `node:sqlite`），新增 `ai.js` 暴露 `/api/ai/chat`、`/api/ai/generate-plan`。
- **RAG**：把「方案模板库 + 临床指南（肌少症/肥胖）+ 设备处方手册」向量化入库，检索增强，减少幻觉、保证严谨可落地。
- **规则闸门（关键）**：AI 输出必经现有规则层校验（禁忌证、强度上限、年龄限制），非法则拦截/警告；最终由医护/教练审核确认才落库（human-in-the-loop）。

### 4.4 提示词与上下文构造
- `context` = 评估 JSON（脱敏字段：gender/age/bmi/grip/gait/1RM/risk）+ 检索到的模板片段 + 指令（「产出符合 schema 的方案，强度必须引用 1RM，列出禁忌」）。
- 输出 JSON，便于前端直接渲染成 `plan` 对象。

### 4.5 安全与合规
- 健康数据不出本地网络；日志脱敏。
- 所有 AI 方案标注「AI 辅助生成，需专业人员确认」。
- 聊天含医学免责声明；危急红 flag 引导就医。

### 4.6 实施路线
- **P0**：本地 Ollama 跑通 Qwen + `/api/ai/chat` 问答面板。
- **P1**：`/api/ai/generate-plan` 产出 JSON 方案 + 规则校验 + 预览编辑。
- **P2**：RAG 接入模板/指南库。

---

## 5. 小Qoo 系统宠物（可拖动 + 互动）

### 5.1 架构（跨页持久）
- 在 `index.html` 加载独立全局脚本 `qoo.js`（单例），在 `body` 末尾挂 `<div id="qoo">`（fixed 定位），**置于 SPA 路由容器之外** → 切换页面/路由时不被销毁，天然跨页。
- z-index 高于普通内容、低于弹窗/模态。

### 5.2 拖动实现（Pointer Events 统一鼠标/触摸）
- `pointerdown` 记录偏移；`pointermove` 更新 `transform: translate(x,y)`；`pointerup` 结束。
- 边界 clamp 到视口；松手可吸附最近边缘（snap）。
- 位置持久化 `localStorage('qoo_pos')`，下次恢复。
- 双击/长按切换「收起」（最小化到边角）。

### 5.3 互动状态机
- **idle**：轻微浮动/眨眼（CSS keyframes）；间隔随机冒泡小提示（如「记得今天做 10 分钟徒手训练哦~」）。
- **dragging**：暂停动画。
- **talk**：点击 → 弹出气泡，含问候 / 健康贴士 / 导航快捷（去评估、看方案）/ 小游戏（接住下落的健康食物）。
- **hidden**：隐藏按钮。

### 5.4 与系统联动
- 评估完成 → Qoo 庆祝动画 + 「太棒了，已为你生成方案！」
- 方案有 AI 风险提示 → Qoo 提示「请让专业人员确认哦」
- 点击 Qoo 直接唤起 AI 聊天面板（与第 4 点打通）。

### 5.5 实现要点与降级
- 用 SVG/CSS 画 Qoo（轻量、可动）；尊重 `prefers-reduced-motion` 关动画。
- 不影响可访问性（aria-hidden、可聚焦关闭）。
- 移动端：触摸拖动；小屏自动收起。

---

## 6. 关键决策点（需你拍板）
1. **AI 部署**：本地自托管（推荐，隐私）/ 云 API / 混合？—— 影响架构与硬件。
2. **配图生成**：build-time 预生成（一次性，推荐）/ 运行时按需？—— 影响是否需常驻图像服务。
3. **优先级**：先落地「匹配逻辑 + 内容重构」（最核心），还是先上 AI/宠物（更炫）？
4. **设备处方强制绑定**：是否强制绑定客观测试（等速/等张），无测试时降级为徒手？—— 影响严谨度与覆盖率。

## 7. 建议实施路线图
- **阶段一（方案内核）**：重写匹配规则引擎 + 设备/徒手内容库 + schema 扩展。（最核心，先落地）
- **阶段二（配图）**：资产清单 + POC 5 张 + 批量生成 + 绑定。
- **阶段三（AI）**：本地 Ollama Qwen + 聊天面板 → 方案生成 + 规则闸门。
- **阶段四（宠物）**：qoo.js 全局浮层 + 拖动 + 互动 + 联动。
